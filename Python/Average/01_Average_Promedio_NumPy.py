# Instalar primer paquete de Python
# Usar biblioteca Numpy (permite procesar, números, registros y objetos)
# url:https://pypi.org/
# buscar paquete, en este caso Numpy,
# abrir terminal y ejecutar -> pip install numpy

import numpy # Importamos biblioteca, pip install numpy

lista_números = [1, 2, 3, 4, 5, 6] # Crear lista

print(numpy.mean(lista_números)) # Imprimir Función promedio

